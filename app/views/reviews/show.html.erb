
<h2>Review:</h2>

<%= @review.user.email %><br>
<%= @review.stick.brand %><br>
<%= @review.review %><br>

<% if @review.user_id == current_user.id %>
 <%= button_to "Delete", stick_review_path(@review),  :method => :delete  %>
 <%= link_to "Edit", edit_stick_review_path(@review.stick, @review)%>
<% end %>

